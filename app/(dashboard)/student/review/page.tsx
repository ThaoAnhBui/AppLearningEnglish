import Link from 'next/link';import {prisma} from '@/lib/db/prisma';import {requireRole} from '@/lib/auth/queries';export default async function Page(){const u=await requireRole('STUDENT');const lessons=await prisma.lesson.findMany({where:{currentPublishedVersionId:{not:null}},include:{currentPublishedVersion:true}});const counts=await prisma.studentProgress.groupBy({by:['flashcardId'],where:{studentId:u.id,due:{lte:new Date()}},_count:true});return <div><h2 className="text-2xl font-bold mb-5">Ôn tập</h2><div className="space-y-3">{lessons.map(l=><Link key={l.id} href={`/student/review/${l.id}`} className="block border rounded-xl p-4"><b>{l.title}</b><p className="text-sm text-muted-foreground">Published v{l.currentPublishedVersion?.versionNumber}</p></Link>)}</div></div>}

import {requireRole} from '@/lib/auth/queries';import {ImportForm} from './import-form';export default async function Page(){await requireRole('TEACHER','ADMIN');return <div className="max-w-2xl"><h2 className="text-2xl font-bold mb-2">Import Excel</h2><p className="text-sm text-muted-foreground mb-5">Cột bắt buộc: front_text, back_text. Tùy chọn: example_sentence.</p><ImportForm/></div>}

import { createClient } from '@supabase/supabase-js'; import { PrismaClient } from '@prisma/client';
const prisma=new PrismaClient(); async function main(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL!,key=process.env.SUPABASE_SERVICE_ROLE_KEY!,email=process.env.SEED_TEACHER_EMAIL!,password=process.env.SEED_TEACHER_PASSWORD!;if(!url||!key||!email||!password)throw new Error('Thiếu biến môi trường seed.');const s=createClient(url,key,{auth:{autoRefreshToken:false,persistSession:false}});const {data,error}=await s.auth.admin.createUser({email,password,email_confirm:true});if(error)throw error;await new Promise(r=>setTimeout(r,300));await prisma.profile.update({where:{id:data.user.id},data:{role:'TEACHER'}});console.log('Teacher created:',email)}main().finally(()=>prisma.$disconnect());
